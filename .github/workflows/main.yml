name: hello world # 해당 actions 이름

on: [push]    # 이벤트 타입

jobs:
  build:
    
    runs-on: ubuntu-latest  # 실행할 컴퓨터 운영체제
    
    steps:  # 작업단위
    
      - name: Run pwd #작업 이름
        run: pwd      # 실제 작업 명령어
      - name: Run ls -al
        run: ls -al
