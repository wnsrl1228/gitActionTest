name: CI

on: [push]    # 이벤트 타입

jobs:
  build:
    
    runs-on: ubuntu-latest  # 실행할 컴퓨터 운영체제
    
    steps:  # 작업단위
    
      - name: Context
        env:  # 환경변수
          COMMIT_ID: ${{ github.sha }} #깃헙에서 제공하는 데이터
        run: echo "Commit id => $COMMIT_ID"
